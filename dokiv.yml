# dest directory
output: ./site

# layouts 文件目录
layout: ./example/layouts

# layouts 文件目录
plugins: ./example/plugins/*.js

# 静态文件如图片等
static: ./example/static

# markdown 文件
docs:
  - ./example/**/*.md

# router mode: `hash` or `history`
mode: 'history'
